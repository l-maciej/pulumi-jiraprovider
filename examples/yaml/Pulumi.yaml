name: provider-jiraprovider
runtime: yaml
plugins:
  providers:
    - name: jiraprovider
      path: ../../bin

resources:
  pulumites2t: #diffenet thing that grpname below
    type: jiraprovider:JiraGroup
    properties:
      JiraGroupName: "1pulumitest"
