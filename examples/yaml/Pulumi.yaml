name: provider-jiraprovider
runtime: yaml
plugins:
  providers:
    - name: jiraprovider
      path: ../../bin

resources:
  pulumites2t: #Logcial name
    type: jiraprovider:JiraGroup
    properties:
      JiraGroupName: "pulumitest"
